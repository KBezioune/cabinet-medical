## Agent skills

### Issue tracker

Issues tracked as GitHub Issues in KBezioune/cabinet-medical, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
